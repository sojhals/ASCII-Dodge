# ASCII-Dodge
A tiny terminal game written in Python, built as an experiment in real-time rendering and collision detection using nothing but math and ASCII characters.  You control a circle (*) and have to dodge an incoming obstacle (@) that flies across the screen — getting faster every time it resets.
